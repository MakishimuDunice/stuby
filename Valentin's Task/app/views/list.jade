ol
   each entry in entries
      li
        span.name Short url:
         a(href=entry.short_url)= entry.short_url
         br
        span.title Long url:
         a(href=entry.long_url)= entry.long_url
        input.delete(type="button", value="X", id=entry._id)
         